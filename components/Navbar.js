export default function navbar(props)
{
    return(
    <nav>
        <span className="TemperatureNav">Temperature</span>
        <span className="HumidityNav">Humidity</span>
        <span className="StatusNav">Status</span>
    </nav>
    )}